{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Zip Addon",
			"type": "shell",
			"command": "rm -f ../${workspaceFolderBasename}.zip && zip -r ../${workspaceFolderBasename}.zip .",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": []
		}
	]
}
